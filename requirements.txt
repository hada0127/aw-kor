# Game Wars GBA 한글화 프로젝트 Python 의존성
#
# 설치 방법:
#   pip install -r requirements.txt
#
# 대부분 도구는 표준 라이브러리만 쓰지만, 폰트/글리프/스프라이트/화면 캡처
# 처리는 Pillow(PIL)가 필요합니다(메인 빌드 build_korean_full.py 포함).

# 코어 요구사항
Python>=3.9
Pillow>=9.0        # 글리프 렌더, 스프라이트/화면 이미지 처리 (from PIL import ...)

# 개발 도구 (선택사항)
# pytest>=7.0       # 단위 테스트
# black>=22.0       # 코드 포매팅
# pylint>=2.0       # 코드 분석
# flake8>=4.0       # 스타일 검사

# 문서 생성 (선택사항)
# sphinx>=4.0       # 문서 생성
# markdown>=3.0     # Markdown 처리
